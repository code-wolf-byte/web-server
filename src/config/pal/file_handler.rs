use std::fs;
fn load_config() -> config::Config {
    let mut data = String::new();
    #[cfg(taget_os == "windows")]{
        
    }

    #[cfg(taget_os == "linux")]{

    }
    #[cfg(target_os == "macos")]{
        
    }
    
}